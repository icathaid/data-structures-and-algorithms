'use strict';
const kth = require('../lib/ll_kth_from_end.js');

describe('Linked List search method', () => {
  it('should generate an empty LL when called with no arguments', () =>{
    let ted = -1;
    expect(ted).toEqual(-1);
  });
});